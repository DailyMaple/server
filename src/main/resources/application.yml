spring:
  profiles:
    active: local

springdoc:
    swagger-ui:
      path: api
      tags-sorter: alpha
      operations-sorter: alpha
    paths-to-match:
      - /**

security:
  jwt:
    token:
      secret-key: ${SECRET_KEY}
      expire-length: ${EXPIRE_LENGTH}


server:
  port: 8080